version: '3.8'

services:
  db:
    image: postgres:15
    container_name: label_postgres
    restart: always
    env_file:
      - .env 
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: